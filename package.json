{
  "name": "bioviz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "benchmark": "node benchmarks/run.js",
    "replay": "node benchmarks/replay.js",
    "start": "python3 -m http.server 8765 --bind 127.0.0.1",
    "test:browser": "node benchmarks/browser-smoke.cjs",
    "build": "node scripts/build-static.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
