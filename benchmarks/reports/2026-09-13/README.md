# Saved benchmark reports — 2026-09-13

These are snapshots of local benchmark outputs. HTML reports contain numeric plots, with no source video or face images. Download the repository and open an HTML report locally; GitHub displays HTML source rather than running the viewer.

- [HR gate calibration](hr-calibration/report.md): experimental thresholds; not adopted.
- [MPU Toolbox benchmark](mpu-toolbox/report.html): 15 selected clips, five recordings; recording8 duplicates the earlier development sample.
- [UBFC control results](control/results.json) and [streaming stage scores](control/stage-scores.json).
- [Kaggle comparison](kaggle-subject1/report.html).
- [Earlier MPU sample comparison](mpu-sample/report.html).

`manifest.json` records snapshot file hashes. Original result metadata retains local source paths and hashes for provenance; those paths are not portable downloads. Raw datasets, private camera diagnostics, model weights and Python environments are not included. See the repository root HANDOFF.md for resuming work and benchmark documentation for dataset access and commands.
